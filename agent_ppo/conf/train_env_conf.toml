[env_conf]
# 训练默认只用 1~8 号图；9~10 号图由 workflow 定期拿来做验证。
map = [1, 2, 3, 4, 5, 6, 7, 8]
map_random = true

# 以下值只是基础值；真正训练时 workflow 会按阶段动态覆盖。
treasure_count = 10
buff_count = 2
buff_cooldown = 200
talent_cooldown = 100
monster_interval = 300
monster_speedup = 500
max_step = 2000
